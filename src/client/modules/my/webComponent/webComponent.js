import { LightningElement, api } from 'lwc';

export default class WebComponent extends LightningElement { //here we'll get the information for the resource. , when clicking

    @api lightningComponent = [] ; 
    
}
